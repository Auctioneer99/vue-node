class ProfileData {
  constructor(firstname, lastname, address, about) {
    this.firstname = firstname;
    this.lastname = lastname;
    this.address = address;
    this.about = about;
  }
}
export default ProfileData;
